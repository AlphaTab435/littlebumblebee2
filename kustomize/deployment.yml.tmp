apiVersion: a
kind: Deploym
metadata:
  name: bankiployment
  labels:
    app: bankeployment
spec:
  replicas: 1r of desired replicas (pods)
  selector:
    matchLabe
      app: ba-deployment
  template:
    metadata:
      labels:
        app: pp-deployment
    spec:
      contain
      - name:app-deployment
        imagedtabish/bankist-app:latest  # Replace with your Docker image and tag
        ports
        - cont: 80  # Port your application listens on
      # You cre containers here if needed
